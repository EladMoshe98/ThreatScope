# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Env / secrets
.env
*.local

# Model weights (large / not for VCS) — keep the folder README only
models/*
!models/README.md

# Logs
*.log

# Benchmark dataset + generated outputs (prep only — not committed)
benchmark/*.rar
benchmark/*.zip
benchmark/*.txt
benchmark/results/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Runtime request log — keep the folder README only
logs/*
!logs/README.md

# Local-only tests (not part of the submission)
tests/
.pytest_cache/
